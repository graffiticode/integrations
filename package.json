{
  "name": "@graffiticode/integrations",
  "version": "1.0.0",
  "description": "Monorepo for Graffiticode MCP server integrations",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "prettier": "^3.1.1",
    "eslint": "^8.55.0",
    "@typescript-eslint/parser": "^6.13.2",
    "@typescript-eslint/eslint-plugin": "^6.13.2",
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/graffiticode/integrations.git"
  },
  "keywords": [
    "graffiticode",
    "mcp",
    "model-context-protocol",
    "monorepo"
  ],
  "author": "Graffiticode",
  "license": "MIT"
}
